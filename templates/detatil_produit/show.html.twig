{% extends 'base.html.twig' %}

{% block title %}DetatilProduit{% endblock %}

{% block body %}
    <h1>DetatilProduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detatil_produit.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ detatil_produit.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_detatil_produit_index') }}">back to list</a>

    <a href="{{ path('app_detatil_produit_edit', {'id': detatil_produit.id}) }}">edit</a>

    {{ include('detatil_produit/_delete_form.html.twig') }}
{% endblock %}
