{% extends 'base.html.twig' %}

{% block title %}New User |{% endblock %}

{% block body %}
{#    <h1>Create new User</h1>#}

{#    {{ include('user/_form.html.twig') }}#}

{#    <a href="{{ path('app_user_index') }}">back to list</a>#}
{#    #}



    <div class="col-12">
        {{ form_start(form) }}
        <div class="card card-fluid">
            <div class="card-header bg-soft-custom" id="bg">
                <h6> <i class="fa-solid fa-list" style="font-size:20px;"></i> Enregistrement des utilisateurs </h6>
            </div>
            <div class="card-body">
                {{ include('user/_form.html.twig') }}
            </div>
            <div class="card-footer">
                <div class="card-footer-content">
                    <button type="submit" class="btn btn-primary float-right ml-2">Enregistrer</button>
                    <a href="{{path('app_user_index')}}" class="btn btn-info float-right">Liste utilisateurs </a>
                </div>
            </div>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
