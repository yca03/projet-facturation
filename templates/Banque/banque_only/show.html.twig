{% extends 'base.html.twig' %}

{% block title %}BanqueOnly{% endblock %}

{% block body %}
    <h1>BanqueOnly</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ banque_only.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ banque_only.nom }}</td>
            </tr>
            <tr>
                <th>CodeBanque</th>
                <td>{{ banque_only.codeBanque }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_banque_only_index') }}">back to list</a>

    <a href="{{ path('app_banque_only_edit', {'id': banque_only.id}) }}">edit</a>

    {{ include('Banque/banque_only/_delete_form.html.twig') }}
{% endblock %}
